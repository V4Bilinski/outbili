import { cn } from '../../lib/cn'

export function WhatsAppIcon({ className }: { className?: string }) {
  return <i className={cn('fi fi-brands-whatsapp leading-none flex items-center justify-center', className)} />
}
