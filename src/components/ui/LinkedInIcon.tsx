import { cn } from '../../lib/cn'

export function LinkedInIcon({ className }: { className?: string }) {
  return <i className={cn('fi fi-brands-linkedin leading-none flex items-center justify-center', className)} />
}
