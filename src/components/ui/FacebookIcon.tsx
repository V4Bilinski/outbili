import { cn } from '../../lib/cn'

export function FacebookIcon({ className }: { className?: string }) {
  return <i className={cn('fi fi-brands-facebook leading-none flex items-center justify-center', className)} />
}
