# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
n8n/*.json

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem
.mcp.json
.dev.vars

# Cloudflare Workers (WIP local + legado). wrangler.toml tem secrets inline
# (ASSERTIVA_CLIENT_ID/SECRET) — NUNCA commitar. Credenciais ficam só em secrets.
outbili-workers/

# Supabase (estado efêmero — schema versionado fica em supabase/migrations/)
supabase/.temp/
supabase/.branches/

# Dependencies (AIOX)
node_modules/

# Build & Logs (AIOX)
dist/
build/
logs/

# IDE & OS (AIOX)
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt
.vercel

# Temporary screenshots & tool caches
*.png
*.jpeg
.playwright-mcp/
playwright/
.firecrawl/
dashboard-snapshot.md
