# Story W3-10 — Remover n8n da busca (modo `mass`) + limpar docs/deploy do sistema

> **Epic:** [`EPIC-airtable-to-supabase-migration`](./epics/EPIC-airtable-to-supabase-migration.md) · **Wave:** W3 (Cutover) · **Story:** W3-10
> **Criada em:** 2026-05-25 · **Criada por:** @sm (River) · **Origem:** split da W3-09 (Fase B)
> **Branch sugerido:** `feature/W3-10-remover-n8n-busca`

```yaml
metadata:
  story_id: W3-10
  story_points: 8          # Standard-alto (refactor de subtração com acoplamento a fluxo vivo)
  executor: '@dev'
  design_authority: '@architect'   # mapa de desacoplamento mass↔specific
  quality_gate: '@qa'              # OBRIGATÓRIO — validar os 3 modos de busca (pesca/specific/mass-removido)
  quality_gate_tools:
    - playwright            # validar SearchPage nos modos pesca + specific (sem regressão)
    - coderabbit            # refactor grande de UI
  complexity_dimensions:
    scope: 4               # SearchPage (~500 linhas) + 2 arquivos deletados + envs + deploy.yml + ~10 docs
    integration: 1         # remoção pura, sem nova integração
    infrastructure: 2      # deploy.yml via MCP github (scope workflow)
    knowledge: 2           # código auditado na sessão W3-09
    risk: 4                # mexe na SearchPage de produção; modo `specific` VIVO acoplado via massEnrichment/history
  complexity_class: STANDARD   # score 13
  decisions:
    cnpja_n8n_enum: 'MANTER — metadado histórico de origem de enriquecimento, NÃO é invocação n8n (operador, 2026-05-25)'
    doc_scope: 'Limpeza documental restrita aos docs do SISTEMA OUTBILI; skill n8n, squads e framework AIOX intactos; stories preservadas como histórico (operador, 2026-05-25)'
    deploy_yml: 'Consolidar aqui a limpeza do deploy.yml (envs Assertiva órfãs da Fase A + envs n8n) via MCP github (scope workflow)'
```

---

`Ready — validada @po (GO 10/10); aguardando implementação @dev (próxima sessão)`

---

## Story

**Como** engenheiro responsável pelo OUTBILI,
**quero** remover por completo o vestígio do n8n da busca de leads (o modo `mass` da SearchPage, o hook `useN8nSearch` e o cliente `n8n-webhook`) e erradicar as referências ao n8n da documentação operacional e do pipeline de deploy do sistema,
**para que** o n8n fique 100% descontinuado — nenhum fluxo do OUTBILI o invoca ou solicita execução a ele — sem qualquer regressão no modo de busca vivo (PESCA) nem no cadastro manual (`specific`).

---

## Contexto

- **Origem:** Fase B da [W3-09](./W3-09.eliminar-n8n-worker.story.md). A Fase A (Edge Function `assertiva-proxy`) já está em produção (`abea343`); o n8n já não é invocado por nenhum fluxo **acessível ao usuário**. Esta story remove o código morto e os resquícios.
- **Revalidação de fluxo (Orion, 2026-05-25):** auditoria do sistema inteiro confirmou que a **única invocação operacional do n8n** é a busca por filtros (modo `mass`), **inalcançável pela UI** (não há botão `setSearchMode('mass')`; o default é `pesca`). Logo a remoção é segura — não derruba fluxo ativo.
- **`cnpja_n8n` NÃO é n8n operacional:** é um valor de enum/status histórico de leads já enriquecidos (`types/index.ts`, `supabase.types.ts`, migration `…224`, `LeadsPage.tsx:159`). **MANTER** — removê-lo quebraria leads legados e o enum no banco.

---

## Análise de desacoplamento (@architect — Aria)

> **A armadilha desta story.** O modo `mass` (n8n) é código morto, MAS `massEnrichment` e `history` são **compartilhados** com o modo `specific` (VIVO). Remover o bloco `mass` cegamente quebra o cadastro manual. Mapa exato (SearchPage.tsx, auditado 2026-05-25):

### A REMOVER (exclusivo do modo `mass`/n8n)
| Item | Linha(s) | Observação |
|------|----------|------------|
| `import { useN8nSearch }` | 11 | |
| `const n8n = useN8nSearch()` | 274 | |
| `useEffect` auto-trigger por `n8n.phase`/`n8n.leads` | 279–294 | dispara `massEnrichment.enrichAll` após busca n8n |
| `isSearching` baseado em `n8n.phase` | 612 | recalcular ou remover |
| `handleSearch` (`n8n.search({...})`) | 614–620 | disparo da busca n8n |
| Bloco `{searchMode === 'mass' && (<>…)}` | 1309–1652 | form pesquisa em massa + UI progresso n8n |
| Empty states / refs `n8n.*` remanescentes | 1470, 1479, 1510–1579, 1589–1652, 1833–1838 | incluindo history panel atrelado a `n8n.phase` |
| Tipo `'mass'` do union `searchMode` | 238 | vira `'specific' \| 'pesca'` |

### A PRESERVAR (vivo — modo `specific` e/ou `pesca`)
| Item | Por quê |
|------|---------|
| `massEnrichment` (`useMassEnrichment`) + painel de progresso (1655–1831) + `pendingResume`/resume (300) | `handleSpecificSearch` dispara `massEnrichment.enrichAll` (588). O painel renderiza FORA do bloco `mass`. **Desacoplar com cuidado.** |
| `<PescaPanel/>` (914) e bloco `specific` (917–1306) | modos vivos |
| `cnpja_n8n` enum (types/migration/LeadsPage) | metadado histórico |

### Decisão de design (`history`)
- `history`/`saveToHistory`/`loadHistory`/`loadFromHistory` (70–89, 237, 616–618, 873, 1479–1486) armazenam **filtros do modo `mass`** (segments/states/city/keywords). Sem o modo `mass`, o histórico fica sem produtor nem consumidor útil.
- **@architect decide na implementação:** remover `history` por completo OU confirmar se algum outro modo o reusa. Default: **remover** (é específico do `mass`), validando que `pesca`/`specific` não o referenciam.

---

## Acceptance Criteria

### AC-1 — Remover o modo `mass` da SearchPage sem regressão
- [ ] Remover `import`/`const n8n`, `useEffect` auto-trigger (279–294), `handleSearch`, `isSearching` (n8n), o bloco `{searchMode === 'mass'}` e todas as refs `n8n.*`.
- [ ] `searchMode` passa a ser `'specific' | 'pesca'` (default `pesca`).
- [ ] `massEnrichment` (painel de progresso + resume + `pendingResume`) **preservado e funcional** para o modo `specific` (cadastro manual dispara `enrichAll`).
- [ ] `history` resolvido conforme decisão @architect (remover ou preservar) — sem código órfão.
- [ ] `tsc -b` sem erros; nenhuma variável/import órfão (eslint `no-unused-vars` limpo).

### AC-2 — Deletar arquivos n8n
- [ ] `src/hooks/useN8nSearch.ts` deletado.
- [ ] `src/lib/n8n-webhook.ts` deletado.
- [ ] `grep -rniE "useN8nSearch|n8n-webhook|triggerN8nSearch" src/` = 0 ocorrências.

### AC-3 — Remover envs n8n + envs Assertiva órfãs (deploy.yml via @devops)
- [ ] Remover `VITE_N8N_WEBHOOK_URL` de `.env.local`, `.env.local.example`; remover `N8N_API_KEY`/`N8N_WEBHOOK_URL` de `.env`, `.env.example`.
- [ ] **`deploy.yml` (via MCP github — scope `workflow`):** remover `VITE_N8N_WEBHOOK_URL` (38), `VITE_ASSERTIVA_CLIENT_ID` (40), `VITE_ASSERTIVA_CLIENT_SECRET` (41), `VITE_ASSERTIVA_WORKER_URL` (42), `VITE_N8N_ASSERTIVA_PROXY` (43). Manter `VITE_CNPJA_API_KEY` (39).
- [ ] Remover secrets GitHub órfãos (`VITE_ASSERTIVA_*`, `VITE_N8N_*`) em Settings → Secrets (@devops).
- [ ] `grep -rniE "VITE_N8N|N8N_API_KEY|VITE_ASSERTIVA" src/ .env*` = 0.

### AC-4 — Limpar docs do SISTEMA OUTBILI (escopo restrito)
- [ ] Remover/atualizar refs a n8n em: `OUTBILI.md` (linha 79 `lib/n8n-webhook`, 151 `VITE_N8N_WEBHOOK_URL`), `README.md`, `docs/guides/GAP-ANALYSIS-OUTBILI.md`, `docs/guides/OUTBILI-SYSTEM-GUIDE.md`, `docs/migration/{NEXT-SESSION-KICKOFF,SESSION-HANDOFF-2026-05-20,SESSION-HANDOFF-2026-05-24,W3-08-DEEP-ENRICHMENT,W3-COMPLETION-REPORT}.md`.
- [ ] **PRESERVAR (fora de escopo):** `docs/stories/*` (histórico append-only), `.claude/skills/n8n/**`, `.claude/commands/n8n-expert.md`, `squads/**`, `.aiox-core/**` (framework L2).
- [ ] `grep -rniE "n8n" OUTBILI.md README.md docs/guides/ docs/migration/` retorna apenas menções históricas explicitamente datadas (ex.: "eliminado em W3-09/W3-10"), nada operacional.

### AC-5 — QA dos 3 modos de busca (sem regressão)
- [ ] **PESCA:** busca por segmento retorna/dedup/salva leads (Edge/CNPJa) — screenshot `playwright/fix/w3-10-pesca-ok.png`.
- [ ] **Specific:** cadastro manual por CNPJ enriquece (CNPJa+Assertiva) e o **painel de progresso do `massEnrichment` renderiza** — screenshot `playwright/fix/w3-10-specific-massenrich-ok.png`.
- [ ] Confirmar na aba Network: nenhuma chamada a `n8n.bilinski.cloud`/`workers.dev`.

### AC-6 — Build + entrega
- [ ] `npm run build` (`tsc -b && vite build`) verde; `npm run lint` sem erros novos.
- [ ] `grep -rniE "n8n|workers\.dev" src/` = apenas falsos positivos (`cnpja_n8n` enum, `pdf.worker`, `Promise.all`) — documentar lista no Dev Notes.
- [ ] Descartar `outbili-workers/` (legado, gitignored) — **confirmar com operador** se remove do disco ou mantém arquivado local.
- [ ] PR + merge (@devops) → deploy GitHub Pages verde → prod HTTP 200.

### AC-7 — Riscos & rollback
- [ ] **Risco:** quebrar o painel `massEnrichment` no modo `specific`. **Mitigação:** AC-5 (QA do specific antes do merge).
- [ ] **Risco:** editar `deploy.yml` via git push local falha (sem scope `workflow`). **Mitigação:** usar `mcp__github__create_or_update_file` (token MCP V4Bilinski TEM o scope — ver [[outbili-supabase-state]]).
- [ ] **Rollback:** `git revert` do commit de merge; o n8n já está morto (inalcançável), então reverter não reativa risco.

---

## Tasks (execução @dev — ordem sugerida)

1. [ ] **@architect:** confirmar mapa de desacoplamento (history: remover vs preservar) antes de tocar código.
2. [ ] Remover refs `n8n.*` + bloco `mass` + tipo do union (AC-1).
3. [ ] Deletar `useN8nSearch.ts` + `n8n-webhook.ts` (AC-2).
4. [ ] Remover envs n8n/Assertiva órfãs do código (.env*) (AC-3 parcial).
5. [ ] `tsc -b` + `lint` + `vite build` verdes (AC-1/AC-6).
6. [ ] QA dos 3 modos via Playwright (AC-5) — @qa.
7. [ ] Limpar docs do sistema OUTBILI (AC-4).
8. [ ] **@devops:** `deploy.yml` via MCP github + remover secrets órfãos + push + PR + merge + deploy (AC-3/AC-6).

---

## Dev Notes

- **NÃO tocar:** `assertiva-proxy`, `assertiva-enrich`, `social-enrich`, `pescaService` (motor de busca vivo), `cnpja_n8n` enum.
- **`PescaPanel`** é autossuficiente (hooks próprios) — o modo `pesca` não depende de nada do bloco `mass`.
- **`deploy.yml`** só por MCP github (`mcp__github__create_or_update_file`); git push local não tem scope `workflow`.
- **Default de `searchMode`** já é `pesca` — a remoção do `mass` não muda a experiência atual do usuário.

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-05-25 | @sm (River) | Story criada a partir do split da W3-09 (Fase B). Inclui mapa de desacoplamento @architect (mass↔specific via massEnrichment/history) e escopo de limpeza documental restrito ao sistema OUTBILI. |
| 2026-05-25 | @po (Pax) | Validação 10-point: **GO 10/10**. Título claro, ACs testáveis (greps/screenshots), escopo A-REMOVER/A-PRESERVAR explícito, dependências mapeadas (W3-09 Fase A, deploy.yml via MCP, @architect), complexidade STANDARD (13), riscos+rollback documentados (AC-7), alinhada ao epic W3. Status Draft → Ready. |
